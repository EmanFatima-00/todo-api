# todo-api
A3 - To-Do API with FastAPI + PostgreSQL + Docker. Data persistence proof - tasks survive container restart.
# A3 - To-Do API with Data Persistence

## Description
FastAPI + PostgreSQL + Docker based To-Do API. 
Focus of A3 is **Data Persistence Proof** - tasks remain after container restart using Docker Volume.

## How to Run

```bash
docker-compose up --build
